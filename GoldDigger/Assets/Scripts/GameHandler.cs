using System.Collections;
using System.Collections.Generic;
using UnityEngine;



public class GameHandler : MonoBehaviour {

	// Use this for initialization
	void Start () {
        DirtBlock db = new DirtBlock("dirtBlock", 1, 0, 0);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
